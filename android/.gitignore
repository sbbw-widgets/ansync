# Gradle / IDE artifacts
.gradle/
build/
local.properties
.idea/
*.iml
captures/
.cxx/

# Rust native build artifacts
target/

# Gradle wrapper jar — fetched at first `gradle wrapper` run. Bundled
# in the rust-android docker image so callers do not need to run
# `gradle wrapper` manually when using the docker workflow.
gradle/wrapper/gradle-wrapper.jar
