# Install to /etc/modprobe.d/ansync-v4l2loopback.conf
#
# devices=0 loads v4l2loopback in dynamic mode — no /dev/videoN
# nodes are pre-created at boot. ansyncd allocates one per peer at
# connect time via V4L2LOOPBACK_CTL_ADD on /dev/v4l2loopback.
options v4l2loopback devices=0
